[linux]
display_name = "Linux"
asset_name = "perspecta-x86_64-unknown-linux-gnu.tar.xz"
button_label = "Download for Linux"
aria_label = "Download the latest Linux build"
title = "Download for Linux"
source_requirements = [
  "Linux desktop with OpenGL/Vulkan support",
  "Rust toolchain (stable)",
]
shell = "bash"
run_cmd = "cargo run"
release_cmd = "cargo build --release"

[macos]
display_name = "macOS"
asset_name = "perspecta-aarch64-apple-darwin.tar.xz"
button_label = "Download for macOS"
aria_label = "Download the latest macOS Apple Silicon build"
title = "Download for macOS (Apple Silicon)"
fallback_url = "/docs/install/macos/"
fallback_aria_label = "Open macOS install guide"
fallback_title = "macOS install guide"
prebuilt_note = "Packaged build targets Apple Silicon."
source_requirements = [
  "Rust toolchain (stable)",
  "Xcode Command Line Tools",
]
shell = "bash"
run_cmd = "cargo run"
release_cmd = "cargo build --release"

[windows]
display_name = "Windows"
asset_name = "perspecta-x86_64-pc-windows-msvc.zip"
button_label = "Download for Windows"
aria_label = "Download the latest Windows build"
title = "Download for Windows"
source_requirements = [
  "Rust toolchain (stable)",
  "Visual Studio Build Tools (C++ workload)",
]
shell = "powershell"
run_cmd = "cargo run"
release_cmd = "cargo build --release"
